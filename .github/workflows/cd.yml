name: CD

on:
    release:
        types: [published]
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: '3.11'
                  cache: 'pip'

            - name: Install build dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install build twine

            - name: Build package
              run: |
                  python -m build

            - name: Check package
              run: |
                  twine check dist/*

            - name: Upload artifacts
              uses: actions/upload-artifact@v4
              with:
                  name: dist
                  path: dist/

    # docker:
    #   runs-on: ubuntu-latest
    #   needs: build
    #
    #   steps:
    #     - uses: actions/checkout@v4
    #
    #     - name: Build Docker image
    #       run: |
    #         docker build -t cognitive-parliament:latest .
    #
    #     # Add Docker push steps when registry is configured
